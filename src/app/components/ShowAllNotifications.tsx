import React from "react";
import ProtectedRoute from "../ProtectedRoute";

const ShowAllNotifications = () => {
  return <ProtectedRoute>ShowAllNotifications</ProtectedRoute>;
};

export default ShowAllNotifications;
